# Mobile production env — committed so external users can build a personal
# iPhone copy of Didian against the same backend as didian.ai on web.
# Loaded by the `*:prod` scripts via dotenv-cli (see package.json).
EXPO_PUBLIC_API_URL=https://api.didian.ai
EXPO_PUBLIC_WEB_URL=https://didian.ai
