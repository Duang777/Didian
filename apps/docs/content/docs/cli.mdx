---
title: CLI command reference
description: One-page overview of every top-level Didian CLI command. For full usage, run `didian <command> --help`.
---

import { Callout } from "fumadocs-ui/components/callout";

The Didian CLI mirrors almost everything the Web UI can do (create [issues](/issues), assign [agents](/agents), start the [daemon](/daemon-runtimes), and more). This page lists every top-level command with a one-line description. For the full set of flags and examples, run `didian <command> --help`.

## Getting authenticated

Run this the first time you use the CLI to obtain a **personal access token (PAT)**:

```bash
didian login
```

Your browser opens automatically. After you approve in the web app, the CLI saves the PAT (prefixed with `mul_`) to `~/.didian/config.json`. Every subsequent command authenticates with that PAT.

<Callout type="tip">
For CI or headless environments, skip the browser flow: create a PAT in the web app under **Settings → Personal Access Tokens**, then run `didian login --token <mul_...>` to supply it directly.
</Callout>

For the difference between token types, see [Authentication and tokens](/auth-tokens).

## Auth and setup

| Command | Purpose |
|---|---|
| `didian login` | Log in and save a PAT |
| `didian auth status` | Show current login status, user, and workspace |
| `didian auth logout` | Clear the local PAT |
| `didian setup cloud` | One-shot setup for Didian Cloud (login + install daemon) |
| `didian setup self-host` | One-shot setup for a self-hosted backend |

## Workspaces and members

| Command | Purpose |
|---|---|
| `didian workspace list` | List every workspace you can access |
| `didian workspace get <slug>` | Show details for one workspace |
| `didian workspace member list` | List members of the current workspace |
| `didian workspace update <id> --name "..." [--description "..."] [--context "..."] [--issue-prefix "..."]` | Update workspace metadata (admin/owner). Long fields accept `--description-stdin` / `--context-stdin`. |

## Issues and projects

<Callout type="info">
`list` commands (`didian issue list`, `autopilot list`, `project list`, etc.) print short, copy-paste-ready IDs by default — issue keys like `MUL-123` for issues, short UUID prefixes for the rest. The `<id>` argument on the follow-up commands below accepts either the short ID or the full UUID, so the typical flow is `didian issue list` → copy the key → `didian issue get MUL-123`. Pass `--full-id` to a list command when you need the canonical UUID.
</Callout>

| Command | Purpose |
|---|---|
| `didian issue list` | List issues (prints copy-paste-ready issue keys) |
| `didian issue get <id>` | Show a single issue (accepts an issue key or a UUID) |
| `didian issue create --title "..."` | Create a new issue |
| `didian issue update <id> ...` | Update an issue (status, priority, assignee, etc.) |
| `didian issue assign <id> --to <name>` | Assign to a member, agent, or squad (assigning to an agent triggers a run) |
| `didian issue status <id> <status>` | Shortcut to change status |
| `didian issue search <query>` | Keyword search |
| `didian issue children <id>` | List sub-issues grouped by stage |
| `didian issue pull-requests <id>` | List linked pull requests and their status |
| `didian issue runs <id>` | Show agent runs on an issue |
| `didian issue run-messages <task-id>` | Show messages for one execution |
| `didian issue usage <id>` | Show aggregated token usage for an issue |
| `didian issue rerun <id>` | Re-enqueue a fresh task for the issue's current agent assignee |
| `didian issue cancel-task <task-id>` | Cancel a queued or running task |
| `didian issue comment <id> ...` | Nested: view / post comments |
| `didian issue comment resolve/unresolve <comment-id>` | Mark a comment thread resolved or unresolved |
| `didian issue subscriber <id> ...` | Nested: subscribe / unsubscribe |
| `didian issue metadata <id> ...` | Nested: read / write issue metadata |
| `didian issue label <id> ...` | Nested: manage labels on an issue |
| `didian project list/get/create/update/delete/status` | Project CRUD |
| `didian label list/create/update/delete` | Workspace label CRUD |

## Agents and skills

| Command | Purpose |
|---|---|
| `didian agent list` | List the workspace's agents |
| `didian agent get <slug>` | Show an agent's configuration |
| `didian agent create ...` | Create an agent |
| `didian agent update <slug> ...` | Update an agent |
| `didian agent archive <slug>` | Archive |
| `didian agent restore <slug>` | Restore an archived agent |
| `didian agent tasks <slug>` | Show an agent's task history |
| `didian agent avatar <slug>` | Upload an agent avatar |
| `didian agent env <slug> ...` | Read or replace an agent's custom environment variables |
| `didian agent skills ...` | Nested: attach / detach skills |
| `didian skill list/get/create/update/delete` | Skill CRUD |
| `didian skill import ...` | Import a skill from GitHub, ClawHub, or the local machine |
| `didian skill files ...` | Nested: manage a skill's files |

### Skill import conflicts

`didian skill import --url <url>` defaults to `--on-conflict fail`. If a skill
with the same name already exists, the command exits with a structured
`conflict` result and does not change the workspace.

Use `--on-conflict overwrite` when you created the existing skill and want to
replace its content while preserving its ID and agent bindings. Use
`--on-conflict rename` to import a copy with an automatic suffix such as `-2`.
Use `--on-conflict skip` to leave the existing skill untouched and report
`skipped`.

```bash
didian skill import --url https://skills.sh/acme/repo/review-helper
didian skill import --url https://skills.sh/acme/repo/review-helper --on-conflict overwrite
didian skill import --url https://skills.sh/acme/repo/review-helper --on-conflict rename
didian skill import --url https://skills.sh/acme/repo/review-helper --on-conflict skip
```

## Squads

| Command | Purpose |
|---|---|
| `didian squad list` | List squads in the workspace |
| `didian squad get <id>` | Show a single squad |
| `didian squad create --name "..." --leader <agent>` | Create a squad (owner / admin) |
| `didian squad update <id> ...` | Update name, description, instructions, leader, or avatar |
| `didian squad delete <id>` | Archive (soft-delete) — transfers assigned issues to the leader |
| `didian squad member list/add/remove/set-role <squad-id>` | Manage squad members and update roles in place |
| `didian squad activity <issue-id> <action\|no_action\|failed> --reason "..."` | Used by squad leader agents to record an evaluation per turn |

See [Squads](/squads) for the full model.

## Autopilots

| Command | Purpose |
|---|---|
| `didian autopilot list` | List every autopilot in the workspace |
| `didian autopilot get <id>` | Show a single autopilot |
| `didian autopilot create ...` | Create an autopilot |
| `didian autopilot update <id> ...` | Update |
| `didian autopilot delete <id>` | Delete |
| `didian autopilot runs <id>` | Show run history |
| `didian autopilot trigger <id>` | Trigger a run manually |
| `didian autopilot trigger-add/update/delete <id>` | Manage schedule or webhook triggers |
| `didian autopilot trigger-rotate-url <id> <trigger-id>` | Rotate a webhook trigger URL |

`didian autopilot create/update` also accepts `--subscriber` to set default
subscribers for issues created by a `create_issue` autopilot; update accepts
`--clear-subscribers` to remove them.

## Daemon and runtimes

| Command | Purpose |
|---|---|
| `didian daemon start` | Start the daemon (background by default; add `--foreground` to run in the foreground) |
| `didian daemon stop` | Stop the daemon |
| `didian daemon restart` | Restart the daemon |
| `didian daemon status` | Check whether the daemon is online and its concurrency |
| `didian daemon logs` | View daemon logs |
| `didian runtime list` | List runtimes in the current workspace |
| `didian runtime usage` | Show resource usage |
| `didian runtime activity` | Recent activity log |
| `didian runtime update <id> ...` | Initiate a CLI update on a runtime |
| `didian runtime delete <id> [--cascade]` | Delete a runtime, optionally archiving bound agents |
| `didian runtime profile ...` | Manage custom runtime profiles and local path overrides |

## Miscellaneous

| Command | Purpose |
|---|---|
| `didian repo checkout <url>` | Clone a repo locally for agents to use |
| `didian config` | View or edit local CLI configuration |
| `didian version` | Print the CLI version |
| `didian update` | Upgrade the CLI to the latest release |
| `didian attachment download <id>` | Download an attachment from an issue or comment |

## Getting full flags

Every command supports `--help`:

```bash
didian issue create --help
didian agent update --help
```

v2 will ship a dedicated detailed reference page for each command.

## Next steps

- [Authentication and tokens](/auth-tokens) — PAT vs. JWT vs. daemon token
- [Daemon and runtimes](/daemon-runtimes) — how the `daemon` commands work under the hood
- [Creating and configuring agents](/agents-create) — all options for `didian agent create`
