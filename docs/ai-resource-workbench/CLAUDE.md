# AI Resource Workbench Docs Guide

Follow `AGENTS.md` in this directory. Keep these docs focused on the current Runtime-first plan:

```text
AI Inbox -> Missions / Codex Run -> Atlas -> System
```

When editing docs here:

- Treat `02-technical-plan.md` as the technical source of truth.
- Treat `03-implementation-review.md` as the build-order source of truth.
- Keep Karakeep only as a linked inspiration: https://github.com/karakeep-app/karakeep . Do not suggest copying its AGPL-3.0 source.
- If you mention AI Studio or Autopilot, make clear they are Advanced / Later, not MVP primary surfaces.
- Update `tasks/plan.md` or `tasks/todo.md` when the implementation plan changes.
- Commit documentation updates atomically after validation.
