---
title: CLI コマンドリファレンス
description: すべてのトップレベル Didian CLI コマンドを 1 ページにまとめた概要です。完全な使い方は `didian <command> --help` を実行してください。
---

import { Callout } from "fumadocs-ui/components/callout";

Didian CLI は、Web UI でできるほぼすべての操作をそのまま提供します（[イシュー](/issues)の作成、[エージェント](/agents)の割り当て、[デーモン](/daemon-runtimes)の起動など）。このページでは、すべてのトップレベルコマンドを 1 行の説明とともに一覧します。フラグや例の完全な一覧は `didian <command> --help` を実行してください。

## 認証する

CLI を初めて使うときにこのコマンドを実行して、**パーソナルアクセストークン（PAT）**を取得します。

```bash
didian login
```

ブラウザが自動的に開きます。Web アプリで承認すると、CLI が PAT（`mul_` プレフィックス付き）を `~/.didian/config.json` に保存します。これ以降のすべてのコマンドはこの PAT で認証されます。

<Callout type="tip">
CI やヘッドレス環境では、ブラウザフローをスキップできます。Web アプリの **Settings → Personal Access Tokens** で PAT を作成し、`didian login --token <mul_...>` で直接渡してください。
</Callout>

トークンの種類による違いについては、[認証とトークン](/auth-tokens)を参照してください。

## 認証とセットアップ

| コマンド | 用途 |
|---|---|
| `didian login` | ログインして PAT を保存 |
| `didian auth status` | 現在のログイン状態、ユーザー、ワークスペースを表示 |
| `didian auth logout` | ローカルの PAT を削除 |
| `didian setup cloud` | Didian Cloud のワンショットセットアップ（ログイン + デーモンのインストール） |
| `didian setup self-host` | セルフホストバックエンドのワンショットセットアップ |

## ワークスペースとメンバー

| コマンド | 用途 |
|---|---|
| `didian workspace list` | アクセスできるすべてのワークスペースを一覧 |
| `didian workspace get <slug>` | 1 つのワークスペースの詳細を表示 |
| `didian workspace member list` | 現在のワークスペースのメンバーを一覧 |
| `didian workspace update <id> --name "..." [--description "..."] [--context "..."] [--issue-prefix "..."]` | ワークスペースのメタデータを更新（admin/owner）。長いフィールドは `--description-stdin` / `--context-stdin` を使用できます。 |

## イシューとプロジェクト

<Callout type="info">
`list` 系のコマンド（`didian issue list`、`autopilot list`、`project list` など）は、デフォルトで短く**そのままコピー＆ペーストできる** ID を出力します。イシューは `MUL-123` のようなイシューキー、それ以外のリソースは短い UUID プレフィックスです。以下の後続コマンドの `<id>` 引数は短い ID と完全な UUID のどちらも受け取るため、一般的な流れは `didian issue list` → キーをコピー → `didian issue get MUL-123` となります。正式な UUID が必要なときは `list` コマンドに `--full-id` を渡してください。
</Callout>

| コマンド | 用途 |
|---|---|
| `didian issue list` | イシューを一覧（コピー＆ペーストできるイシューキーを出力） |
| `didian issue get <id>` | 単一のイシューを表示（イシューキーまたは UUID を受け取る） |
| `didian issue create --title "..."` | 新しいイシューを作成 |
| `didian issue update <id> ...` | イシューを更新（ステータス、優先度、担当者など） |
| `didian issue assign <id> --to <name>` | メンバー、エージェント、またはスクワッドに割り当て（エージェントへの割り当ては実行をトリガー） |
| `didian issue status <id> <status>` | ステータス変更のショートカット |
| `didian issue search <query>` | キーワード検索 |
| `didian issue children <id>` | サブイシューを stage ごとに一覧 |
| `didian issue pull-requests <id>` | 紐付いた pull request と状態を一覧 |
| `didian issue runs <id>` | イシュー上のエージェント実行を表示 |
| `didian issue run-messages <task-id>` | 1 回の実行メッセージを表示 |
| `didian issue usage <id>` | イシュー単位の集計 token 使用量を表示 |
| `didian issue rerun <id>` | イシューの現在のエージェント担当者向けに新しいタスクを再キューイング |
| `didian issue cancel-task <task-id>` | キュー中または実行中のタスクをキャンセル |
| `didian issue comment <id> ...` | ネスト: コメントの表示 / 投稿 |
| `didian issue comment resolve/unresolve <comment-id>` | コメントスレッドを解決済み / 未解決にする |
| `didian issue subscriber <id> ...` | ネスト: 購読 / 購読解除 |
| `didian issue metadata <id> ...` | ネスト: イシューメタデータの読み書き |
| `didian issue label <id> ...` | ネスト: イシューのラベルを管理 |
| `didian project list/get/create/update/delete/status` | プロジェクトの CRUD |
| `didian label list/create/update/delete` | ワークスペースラベルの CRUD |

## エージェントとスキル

| コマンド | 用途 |
|---|---|
| `didian agent list` | ワークスペースのエージェントを一覧 |
| `didian agent get <slug>` | エージェントの構成を表示 |
| `didian agent create ...` | エージェントを作成 |
| `didian agent update <slug> ...` | エージェントを更新 |
| `didian agent archive <slug>` | アーカイブ |
| `didian agent restore <slug>` | アーカイブ済みのエージェントを復元 |
| `didian agent tasks <slug>` | エージェントのタスク履歴を表示 |
| `didian agent avatar <slug>` | エージェントのアバターをアップロード |
| `didian agent env <slug> ...` | エージェントの custom environment variables を読み取り / 置換 |
| `didian agent skills ...` | ネスト: スキルのアタッチ / デタッチ |
| `didian skill list/get/create/update/delete` | スキルの CRUD |
| `didian skill import ...` | GitHub、ClawHub、またはローカルマシンからスキルをインポート |
| `didian skill files ...` | ネスト: スキルのファイルを管理 |

### スキルインポートの競合

`didian skill import --url <url>` の既定値は `--on-conflict fail` です。同じ名前のスキルがすでに存在する場合、コマンドは構造化された `conflict` 結果で終了し、ワークスペースは変更されません。

既存スキルの作成者で、スキル ID とエージェントの紐付けを維持したまま内容を置き換える場合は `--on-conflict overwrite` を使います。既存スキルを残してコピーを取り込む場合は `--on-conflict rename` を使うと、`-2` のような接尾辞が自動で付きます。同名の項目を単に飛ばす場合は `--on-conflict skip` を使います。

```bash
didian skill import --url https://skills.sh/acme/repo/review-helper
didian skill import --url https://skills.sh/acme/repo/review-helper --on-conflict overwrite
didian skill import --url https://skills.sh/acme/repo/review-helper --on-conflict rename
didian skill import --url https://skills.sh/acme/repo/review-helper --on-conflict skip
```

## スクワッド

| コマンド | 用途 |
|---|---|
| `didian squad list` | ワークスペースのスクワッドを一覧 |
| `didian squad get <id>` | 単一のスクワッドを表示 |
| `didian squad create --name "..." --leader <agent>` | スクワッドを作成（owner / admin） |
| `didian squad update <id> ...` | 名前、説明、指示、リーダー、またはアバターを更新 |
| `didian squad delete <id>` | アーカイブ（ソフト削除） — 割り当て済みのイシューをリーダーに移管 |
| `didian squad member list/add/remove <squad-id>` | スクワッドメンバーを管理 |
| `didian squad activity <issue-id> <action\|no_action\|failed> --reason "..."` | スクワッドリーダーエージェントがターンごとに評価を記録するために使用 |

完全なモデルについては[スクワッド](/squads)を参照してください。

## オートパイロット

| コマンド | 用途 |
|---|---|
| `didian autopilot list` | ワークスペースのすべてのオートパイロットを一覧 |
| `didian autopilot get <id>` | 単一のオートパイロットを表示 |
| `didian autopilot create ...` | オートパイロットを作成 |
| `didian autopilot update <id> ...` | 更新 |
| `didian autopilot delete <id>` | 削除 |
| `didian autopilot runs <id>` | 実行履歴を表示 |
| `didian autopilot trigger <id>` | 手動で実行をトリガー |
| `didian autopilot trigger-add/update/delete <id>` | schedule または webhook trigger を管理 |
| `didian autopilot trigger-rotate-url <id> <trigger-id>` | webhook trigger URL をローテート |

`didian autopilot create/update` は、`create_issue` モードの autopilot が作成するイシューの既定購読者を設定する `--subscriber` も受け取ります。`update` では `--clear-subscribers` で削除できます。

## デーモンとランタイム

| コマンド | 用途 |
|---|---|
| `didian daemon start` | デーモンを起動（デフォルトはバックグラウンド。`--foreground` を追加するとフォアグラウンドで実行） |
| `didian daemon stop` | デーモンを停止 |
| `didian daemon restart` | デーモンを再起動 |
| `didian daemon status` | デーモンがオンラインかどうかと同時実行数を確認 |
| `didian daemon logs` | デーモンのログを表示 |
| `didian runtime list` | 現在のワークスペースのランタイムを一覧 |
| `didian runtime usage` | リソース使用量を表示 |
| `didian runtime activity` | 最近のアクティビティログ |
| `didian runtime update <id> ...` | ランタイム上で CLI 更新を開始 |
| `didian runtime delete <id> [--cascade]` | ランタイムを削除し、必要なら紐付くエージェントもアーカイブ |
| `didian runtime profile ...` | カスタム runtime profile とローカルパス上書きを管理 |

## その他

| コマンド | 用途 |
|---|---|
| `didian repo checkout <url>` | エージェントが使用できるようにリポジトリをローカルにクローン |
| `didian config` | ローカルの CLI 構成を表示または編集 |
| `didian version` | CLI のバージョンを出力 |
| `didian update` | CLI を最新のリリースにアップグレード |
| `didian attachment download <id>` | イシューまたはコメントから添付ファイルをダウンロード |

## 完全なフラグを確認する

すべてのコマンドが `--help` をサポートしています。

```bash
didian issue create --help
didian agent update --help
```

v2 では、各コマンドごとに専用の詳細なリファレンスページを提供する予定です。

## 次のステップ

- [認証とトークン](/auth-tokens) — PAT vs. JWT vs. デーモントークン
- [デーモンとランタイム](/daemon-runtimes) — `daemon` コマンドが内部でどう動作するか
- [エージェントの作成と構成](/agents-create) — `didian agent create` のすべてのオプション
